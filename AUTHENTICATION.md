# 🔐 نظام المصادقة والتسجيل الجديد

## ملخص التحديثات

تم تحديث نظام المصادقة في AirRoom ليوفر تجربة أكثر سلاسة وأماناً:

### ✨ الميزات الجديدة

1. **اسم مستخدم فريد** — كل مستخدم يختار اسماً فريداً يعرّفه بشكل دائم
2. **عدم تكرار الاسم** — لا يمكن اختيار اسم مستخدم موجود بالفعل
3. **تحقق فوري** — التحقق من توفر الاسم أثناء الكتابة (في الوقت الفعلي)
4. **ذاكرة المستخدم** — حفظ الاسم في localStorage للدخول السريع
5. **منع التغيير** — لا يمكن تغيير الاسم بعد التسجيل الأول
6. **ثقة مستمرة** — إعادة اتصال تلقائية بنفس الاسم في المرات القادمة

---

## 🔄 سير العملية

### المستخدم الجديد (أول مرة)
```
1. يدخل التطبيق
2. يرى شاشة "إنشاء حساب جديد"
3. يكتب اسم مستخدم
4. التطبيق يتحقق من التوفر فوراً (API request)
5. إذا كان متوفراً: يمكنه الضغط "إنشاء حساب"
6. يتم حفظ الاسم في localStorage
7. يدخل الواجهة الرئيسية
```

### المستخدم الموجود
```
1. يدخل التطبيق
2. يرى شاشة "تسجيل الدخول"
3. يكتب اسم المستخدم الخاص به
4. يضغط "تسجيل الدخول"
5. يتم التحقق من وجود المستخدم في قاعدة البيانات
6. يتم تسجيل الدخول تلقائياً
7. يدخل الواجهة الرئيسية
```

---

## 🔧 التعديلات التقنية

### Backend (server.js)

#### 1. API Endpoint للتحقق الفوري
```javascript
GET /api/check-username/:username
```

**الاستجابة:**
```json
{
  "available": true,
  "username": "أحمد_الجهاز",
  "message": "هذا الاسم متوفر"
}
```

**أو** في حالة عدم التوفر:
```json
{
  "available": false,
  "username": "أحمد_الجهاز",
  "message": "هذا الاسم مستخدم بالفعل"
}
```

#### 2. Socket Event: `reconnect_user`
```javascript
socket.emit('reconnect_user', { username }, (result) => {
  if (result.success) {
    // دخول ناجح
    console.log(result.userId);
  } else {
    // فشل - المستخدم غير موجود
    console.log(result.error);
  }
});
```

**الاستجابة:**
```javascript
{
  success: true,
  userId: "uuid...",
  username: "أحمد_الجهاز",
  deviceId: "uuid..."
}
```

### Frontend (index.html + app.js)

#### 1. دالة `initAuthScreen()`
تتحقق من وجود مستخدم مسجل مسبقاً:
- إذا وُجد: تعرض شاشة الدخول
- إذا لم يُوجد: تعرض شاشة التسجيل الجديد

#### 2. دالة `checkUsernameAvailability(username)`
تحقق فوري من توفر الاسم:
```javascript
// يتم استدعاؤها عند كل كتابة
input.addEventListener('input', (e) => {
  checkUsernameAvailability(e.target.value.trim());
});
```

#### 3. دالة `registerFirstTime()`
تسجيل مستخدم جديد:
```javascript
socket.emit('register_new_user', { username }, callback);
// ثم حفظ في localStorage
localStorage.setItem('airroom_username', username);
```

#### 4. دالة `reconnectUser()`
دخول مستخدم موجود:
```javascript
socket.emit('reconnect_user', { username }, callback);
```

---

## 💾 البيانات المحفوظة

### localStorage
```javascript
{
  'airroom_username': 'أحمد_الجهاز'  // اسم المستخدم فقط
}
```

### قاعدة البيانات (SQLite)
```sql
CREATE TABLE users (
  id TEXT PRIMARY KEY,           -- UUID
  username TEXT UNIQUE NOT NULL, -- اسم المستخدم الفريد
  deviceId TEXT UNIQUE NOT NULL, -- معرّف الجهاز
  createdAt DATETIME,            -- تاريخ التسجيل
  lastLogin DATETIME             -- آخر دخول
)
```

---

## 🔒 الأمان والخصوصية

1. **الفريدية**: كل اسم مستخدم فريد ومشفر في قاعدة البيانات
2. **عدم التخزين المحلي للتفاصيل الحساسة**: فقط الاسم في localStorage
3. **معرّفات الأجهزة**: كل جهاز له معرّف فريد لتتبع الاتصالات
4. **عدم القابلية للتغيير**: اسم المستخدم لا يتغير بعد التسجيل

---

## 📝 أمثلة الاستخدام

### إنشاء حساب جديد
```javascript
// 1. التحقق من التوفر
fetch('/api/check-username/أحمد_الجهاز')
  .then(r => r.json())
  .then(data => {
    if (data.available) {
      // 2. التسجيل
      socket.emit('register_new_user', { username: 'أحمد_الجهاز' }, (result) => {
        if (result.success) {
          localStorage.setItem('airroom_username', result.username);
          // 3. الدخول للواجهة الرئيسية
        }
      });
    }
  });
```

### دخول مستخدم موجود
```javascript
// 1. الاسم موجود بالفعل في localStorage
const username = localStorage.getItem('airroom_username');

// 2. إعادة الاتصال
socket.emit('reconnect_user', { username }, (result) => {
  if (result.success) {
    // 3. الدخول للواجهة الرئيسية
  }
});
```

---

## 🐛 معالجة الأخطاء

### خطأ: اسم مستخدم موجود
```
الرسالة: "❌ هذا الاسم مستخدم بالفعل"
الحل: اختر اسماً آخر
```

### خطأ: اسم قصير جداً
```
الرسالة: "اسم المستخدم يجب أن يكون 3 أحرف على الأقل"
الحل: أضف المزيد من الأحرف
```

### خطأ: المستخدم غير موجود (عند إعادة الاتصال)
```
الرسالة: "هذا المستخدم غير موجود في النظام"
الحل: تأكد من كتابة الاسم بشكل صحيح
```

---

## 🔄 التوافقية مع النسخ القديمة

إذا كان لديك نسخة قديمة من AirRoom:
- المستخدمون الجدد سيستخدمون النظام الجديد مباشرة
- المستخدمون القدماء قد يحتاجون إلى إعادة تسجيل (مرة واحدة فقط)
- جميع البيانات والملفات المنقولة ستبقى آمنة

---

## 📞 الدعم والمساعدة

إذا واجهت مشكلة:
1. تحقق من اتصالك بالإنترنت
2. امسح ذاكرة التطبيق (localStorage) وحاول مرة أخرى
3. تأكد من أن اسم المستخدم لا يحتوي على أحرف خاصة
4. جرب متصفح آخر

---

**آخر تحديث**: مايو 2026
**الإصدار**: 2.0.0 (نظام مصادقة جديد)
